{
  "name": "cal-dependency-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for analyzing CAL (Business Central AL) code dependencies",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "stream-json": "^1.8.0",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
